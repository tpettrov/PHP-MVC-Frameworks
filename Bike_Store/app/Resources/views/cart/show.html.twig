{% extends 'base.html.twig' %}

{% block content %}
    <h1>My Cart</h1>

    <table class="table">
        <tbody>

        <tr>
            <th>Products</th>
            <td>{{ include(':product:show_in_cart.html.twig') }}</td>
        </tr>
        </tbody>
    </table>


    <a class="btn btn-primary" href="{{ path('product_index') }}">Back to the shop</a>

{% endblock %}
